
# 准备数据集结构文件

# 根目录创建1个文件夹(可自定义名称)
# 下面创建再2个文件夹(images和labels)
# images和labels 下再分别创建2个文件夹(train和val)
# images下的train和val 放入训练图片(png、jpg)
# labels下的train和val 放入图片标注(txt)

# 你的项目根目录/
# ├── images/
# │   ├── train/        ← 放训练用的图片（如 .jpg, .png）
# │   └── val/          ← 放验证用的图片
# └── labels/
#     ├── train/        ← 放对应图片的标签文件（.txt 格式）
#     └── val/          ← 放验证集的标签文件

